# PR 43 cost canary, September 13

The full review of `6d1f610763a1d980a325cfaa26eebe1ec7c458fd`
was admitted at 16:35:06 UTC. Shared preparation completed and seven lanes
started at 16:41 UTC. Gateway returned `402 insufficient_funds` at 16:44 UTC.
The aggregate Check reached `action_required` at 16:45:06 UTC and the existing
main comment changed to review incomplete. No Check remained in progress.
This demonstrates terminal failure publication, not completed review quality.

Attempt `a5f9d9e4-d060-484a-9ceb-e8e9c74894e9` retained 146 model-call
observations. Its 136 successful generations independently reconciled to
$20.5620614 in both SDK and Gateway observations. The ten rejected calls have
no generation IDs or resolved billing quantities. The known subtotals are not
additive; a complete attempt total remains unknown under the ledger contract.
Input usage was 13,707,968 tokens, including 10,652,671 cache-read tokens.
No full-plus-delta cost or quality claim follows from this interrupted run.

The runtime delivered unchanged development skill manuals as product requirement
sources because their paths shared words with the change. On the same exact
base/head and 181 changed files, offline discovery after the repair selects 21
sources instead of 48. The removed sources are 27 unchanged development skill
documents selected only by the path-word heuristic. Explicitly configured,
changed, governing and referenced sources remain eligible. No changed-file
manifest entry is removed.

Using the production tokenizer, the requirement index changes from 15,435 to
3,128 tokens. Source text changes from 124,557 to 68,632 tokens, including
delivery of identical base/head text once. These are deterministic payload
measurements, not a projection of total paid savings. Exact source and obligation
IDs remain available, while integrity metadata stays in signed evidence storage.

Retained lane evidence also identified stale custom-lane invocation documentation,
unexplained voice preset letters, duplicated browser-version configuration, and
terminal telemetry overwriting earlier stream cost. The associated fixes preserve
the helper-owned browser version and previously observed usage/cost. The telemetry
regression uses the installed SDK and its official streaming mock. The booking
destination remains an explicit maintainer deferral, not a newly actionable defect.

Before another paid canary, run the complete offline gate and recorded PR 61
transition replay, deploy the repaired runtime, and verify the current key and
team credit separately. A later dashboard balance cannot establish what the
provider observed when the failed requests were admitted.

## Second attempt and retained repairs

The repaired runtime automatically admitted head `98c4114` at 17:02:13 UTC,
without another manual full command. Attempt
`4d282fc1-c4f8-4b22-a085-42d5c27932ec` reached the existing $150 key allowance.
It ended at 17:23:08 UTC with all Checks terminal and the existing main comment
marked incomplete. Four lanes completed; claim/specification, deduplication and
test-against-spec did not. Successful compilation is not a completed review.

The ledger retained 193 call observations and independently reconciled a known
$28.4272528 subtotal in both SDK and Gateway records. Seven rejected requests
still lack billing quantities. A superseded recovery attempt between the two
full attempts reconciled $0.1032942. Together with the first full attempt, the
known subtotal for this resumed session is $49.0926084, not a complete review
price or a proven saving. Model input includes repeated context and cache reads.

Deduplication generated an unterminated, schema-forbidden property in a `bash`
tool call, then repeated whitespace inside that string. The retained stream grew
to 209,221 characters. Cooperative cancellation was acknowledged but did not
settle the stream; retiring the exact child allowed the root to reach terminal
failure. The offline guard rejects the exact recorded stream at character 101,
using the actual tool schema rather than a length or time ceiling. SDK mock tests
verify upstream cancellation, no tool execution, and retained generation identity.

Fatal session errors also skipped turn-level sandbox cleanup. The repaired
session callback stops the root VM with a durable receipt, so later callbacks
do not reopen it. Both observed abandoned review VMs were stopped explicitly.
Repeated failure-publication blobs are reclaimed atomically with pointer changes.

Retained findings drove a separate 3,040-byte landing icon, modern Yarn CLI/cache
support, an incomplete-lockfile triage regression, and concise documentation
corrections. Real Yarn 4.18.0 acquisition and offline materialization passed with
the project hook disabled during acquisition, executed offline, and lock unchanged.
No completed full-plus-delta quality or total-cost comparison is available yet.
